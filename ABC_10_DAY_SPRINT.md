> **文档治理声明**
>
> - 本文件角色：2026-08-14 至 2026-08-30 的挑战杯 A–B–C–D 唯一执行排期（比赛实际截止 08-30）。
> - 改造时间：2026-08-15（Asia/Shanghai）。
> - 原文件去向：[ABC_10_DAY_SPRINT_归档_20260815.md](ABC_10_DAY_SPRINT_归档_20260815.md)。
> - 改造原因：按工程演示优先、离线数据预置、D 可视化闭环和实验 B 延后原则重排任务。
> - 架构依据：[ARCTIC_ROUTE_SYSTEM.md](ARCTIC_ROUTE_SYSTEM.md)。

# 挑战杯 A–B–C–D 冲刺排期（08-14 → 08-30）

## 1. 本轮唯一目标

本轮成功定义为：**在已冻结的场景上，无网重复展示 A 数据、B 风险、C 三目标路线与至少一次
重规划，并由 D 显示风险图、风险预测图、路线和指标。**首选摩尔曼斯克外海—迪克森外海；截至
2026-08-15 已交付 `tromso_isfjorden_august_2026_demo_v1`（12 类/144 h）作为当前演示底座。

```text
A 冻结数据 → B 连续风险序列 → C 三目标/重规划 → D 可视化 → 重复演示
```

风险图和路线应基本合理、无明显荒谬结果。科学标定、真实业务化和适航认证不属于冲刺闸门；
所有输出继续标注 `demo_unvalidated` 和禁止真实导航。

## 2. 冻结范围

| 项目 | 本轮口径 |
|---|---|
| 主航区（首选，未交付） | `offshore_murmansk_to_offshore_dikson` |
| 主航区端点 | 69.15°N, 33.60°E → 73.55°N, 80.40°E |
| 默认数据窗 | 168 h；允许 144–216 h |
| 当前演示（已交付 144 h） | `tromso_to_isfjorden_outer` |
| 船型 | `demo_unvalidated` 演示散货船参数集 |
| B 输出 | 逐小时风险图/序列，覆盖 C 实际 ETA |
| C 输出 | v3 四层 × 三目标为演示主线（已确认 2026-08-15）；v2 三目标为强制后备 |
| D 输出 | 风险现状/预测、候选路线、ETA/距离/风险及重规划变化 |
| 运行方式 | 比赛默认稳定演示；历史回放保留为验证模式 |

> D 选型评估（2026-08-15）：已确认 v3 为演示主线、v2 为强制后备，详见
> [D 展示层选型评估](D_SELECTION_EVALUATION_v2_vs_v3.md)。

> 冻结演示数据集（2026-08-15）：`tromso_isfjorden_august_2026_demo_v1` 已交付
> （12 类/144 h，完整覆盖），交付与恢复见
> [冻结演示数据集交付说明](work_package_a/docs/FROZEN_DEMO_DATASET_DELIVERY.md)。

### 2.2 网络与下载环境注意事项

项目日常使用代理网络。对于 A 的数据下载任务：

- 下载前分别测试“开代理”与“不开代理”两种路径，选择更快、更稳定的一方；不要把代理默认
  视为最快或最慢；
- 部分数据网站存在人机验证或需要登录凭证；AI 无法自行绕过时，把访问失败原因、所需操作和
  备选来源写清楚，由项目负责人手动执行或提供凭证；
- 所有下载请求、失败、凭证问题和源站变化都要保留记录，用于答辩与复现；
- 比赛现场不依赖网络：数据必须提前冻结到本地。

2026-08-15 实测：直连 NOMADS/CMEMS/GEBCO 可用（HTTP 200/307/200）；本地代理
`127.0.0.1:10808` 无效；Copernicus 凭据可用、无需人机验证。12 类/144 h 冻结数据已交付，
见 [冻结演示数据集交付说明](work_package_a/docs/FROZEN_DEMO_DATASET_DELIVERY.md)。


### 2.1 2026-08-14 工程基线（回填快照）

（源自：ABC_10_DAY_SPRINT_归档_20260815.md）以下实测快照用于对照“工程已实现”与“挑战杯
已验收”，不是本轮承诺：fixture 通过不等于实源或科学验收。

| 工程 | 已具备 | 尚未满足的冲刺门禁 |
|---|---|---|
| contracts 0.3.0 | 动态窗、双走廊、bundle v2 复核、RunContext v2；18 tests passed | 真实 bundle 输入 |
| A 0.4.2 | 采集/归档/回放/doctor/exact resolver；172 tests passed | 主走廊真实恰好 12 类、168 h 正式制品 |
| B 0.2.0 | 规则 full/suffix committed 主线；40+8 tests passed | 同一真实 bundle 的 169 帧窗口 |
| B CNN P1 | safetensors、CPU 单步后端、10 model tests passed | P2 未授权/未实现；不进入正式主线 |
| C 0.4.0 | v2/v3、重规划与原子发布；138 passed | 真实 B 窗口的 v2、+6 h 与 v3 证据 |
| orchestrator 0.1.0 | 公共 API 编排；Python 3.13.15 + uv 0.12.4、`uv.lock`、非集成门禁通过 | 集成测试阶段遥测、性能预算、完整收口与两次复现 |
| experimental B 0.1.0 | 现有 `.venv` 中 35 tests passed | stale lock、无有效 Mamba 前缀、Ruff I001；不阻塞正式主线 |

## 3. 工作包分工

| 模块 | 必须完成 | 本轮不做 |
|---|---|---|
| contracts | 保持走廊、场景、船型和 RunContext 一致 | 不吸收 B/C 私有参数 |
| A | 优先尝试交付“恰好 12 类、连续 168 h、来源可查”的冻结数据集；完成不了才降级为来源清楚、连续可用的冻结演示数据 | 不追求比赛现场最新下载 |
| B | 对冻结 A 数据生成连续、视觉合理的风险序列 | 不启动 CNN P2，不生成路线 |
| C | 三目标路线、指标和至少一次重规划 | 不以科学标定阻塞演示 |
| D | 只读展示风险图、预测图、路线和指标 | 不调用 B/C 内部函数 |
| orchestrator | 分阶段运行、报告、缓存与断网复现 | 不吞掉失败，不访问私有数据库 |
| experimental B | 仅记录待修 | 真实主线前不做工程卫生修复，不进入 C |

### 3.1 范围与所有权细化（源自：ABC_10_DAY_SPRINT_归档_20260815.md）

| 所有者 | 本冲刺必须完成 | 明确不做 |
|---|---|---|
| contracts | 保持共享身份、动态窗、12+2 类和 v2 合同稳定 | 不接纳 B/C 私有参数，不把十日写入运行时域 |
| A | 准备稳定冻结数据、bundle/RunContext、doctor 与离线读取 | 可选水深/法规区不阻塞；不统一目标网格 |
| B | 对同一冻结输入构建逐小时 committed window 与失败报告 | 不切换 CNN；不生成最终船速、ETA 或路线 |
| C | v3 四层整组 + 重规划为主验证，v2 三目标为后备回归 | 不重写 A*/成本核心；不读取 A 私有目录 |
| D | 只读展示风险、路线与指标 | 不调用 B/C 内部函数；不持有计算锁 |
| orchestrator | 公共 API 串联、分阶段报告、清单与断网复现 | 不扫描私有数据库/缓存；不吞掉阶段失败 |
| experimental B | 仅在不影响主线时修复工程门禁 | 不进入正式 B store/C，不占用 P0 数据与长运行预算 |

## 4. 工程演示闸门

| 闸门 | 通过条件 | 失败处理 |
|---|---|---|
| G0 身份/环境 | 同一场景、RunContext、generation；各包标准环境可启动 | 停止长运行，修复环境/身份 |
| G1 A 离线制品 | 所需变量和时域可稳定加载；来源、单位、时间和网格不串线 | 换用已冻结可用数据，不现场临时拼接 |
| G2 B 风险 | 60 min 闭区间；full/suffix 原子 commit；身份/摘要一致；缺测/future/stale 均拒绝；非全零/全满；陆海、缺测和时空方向无明显错误 | 回到 A/B 边界定位，不以 synthetic 或 CNN 替代 |
| G3 C 路线 | 三目标齐全；ETA 严格递增；硬约束违规 0；risk IDs 可追溯；不穿陆；无明显荒谬绕行；能重规划 | 保留 v2 失败证据并调整演示参数记录版本，不伪装科学结论 |
| G4 D 展示 | 风险现状/预测、路线与指标同步；不显示旧代次 | 使用最新完整 CD 制品，不等内部函数 |
| G5 稳定复现 | 至少两次无网演示成功；输入与配置摘要一致；报告、JSON/GeoJSON、SHA-256 清单完整；流程和答辩口径一致 | 冻结可用路径，只修阻断问题 |

### 4.1 开发快路径与完整验证路径

主线口径：**v3 四层 × 三目标 + 重规划（已确认 2026-08-15），v2 为强制后备。**

开发快路径（日常，默认离线，复用冻结制品）：

```bash
# 各包日常：L0 单元 + L1 合同 + L2 包内集成（对应包）
cd /root/my_project/work_package_a && make check          # A
cd /root/my_project/work_package_b && make check          # B（不含 model）
cd /root/my_project/work_package_c && UV_OFFLINE=1 make check
# orchestrator 快速门禁（不跑长集成）
cd /root/my_project/arctic_route_orchestrator && make smoke
# orchestrator 局部 smoke（冻结 A bundle → B → C 小窗/低节点）见 handoff
```

完整验证路径（关键里程碑/发布/演示前，可离线用冻结数据）：

```bash
cd /root/my_project/arctic_route_orchestrator && make integration-v2   # v2 后备完整
cd /root/my_project/arctic_route_orchestrator && make integration-v3   # v3 主线完整（8/20 门槛）
cd /root/my_project/arctic_route_orchestrator && make check            # 发布级
```

原则：不重复下载同窗数据、不重复重建未变化的上游冻结制品；所有复用按 digest/provenance
失效（顶层系统文档 §6.3/§10.4）。不得为省时间跳过 v3 整组原子发布约束或改用粗网格冒充
正式验证。

## 5. 执行表（08-14 → 08-30）

| 日期 / Day | A/contracts | B | C | D/orchestrator | 退出条件 |
|---|---|---|---|---|---|
| 08-14 / D1 | 冻结两条航线和动态时域 | 冻结规则主线 | 冻结 v2/v3 合同 | 建立文档与环境基线 | 架构和身份可读 |
| 08-15 / D2 | 冻结挑战杯数据预置方案 | 确认风险图演示指标 | 确认路线合理性指标 | 冻结 D 页面/制品字段 | D1–D7 写入全部入口 |
| 08-16 / D3 | 准备主航区连续本地制品 | 对冻结输入做风险 smoke | 对同一风险做三目标 smoke | 分阶段输出与错误落盘 | G1 候选和端到端小窗存在 |
| 08-17 / D4 | 完成标准化、bundle/RunContext 与离线读取 | 固化逐小时风险窗口 | 修复明显穿陆/时间/端点异常 | D 读取静态风险与路线 | G1、G2 通过 |
| 08-18 / D5 | 冻结输入，不再无关扩源 | 输出主场景风险序列 | v3 整组可运行（v2 后备基线） | 展示风险现状/预测和分层路线 | G3 基线通过 |
| 08-19 / D6 | 提供模拟时钟推进数据 | 生成更新后风险窗口 | 执行至少一次重规划 | 展示更新前后差异及原因 | 重规划闭环通过 |
| 08-20 / D7 | 保存冻结数据与说明 | 冻结规则/参数版本 | 冻结 v3 整组输出（v2 后备） | 第一次完整断网彩排 | G0–G4 通过 |
| 08-21 / D8 | 只读支持 | 只修阻断 | 复验 v3 整组与重规划 | D 完善图例、状态与警示 | 展示可讲解、无旧代次 |
| 08-22 / D9 | 有余量再迁移短航区 | 同参数迁移验证 | 短航区 smoke 或保留延期 | 第二次断网彩排 | G5 通过 |
| 08-23 / D10 | 证据冻结 | 回归 | 回归 | 文档、演示脚本、答辩材料冻结 | 不新增功能，形成最终包 |
| 08-24 / D11 | 按起点阻塞结论补采主走廊或调整方案 | 长场景失败定位 | v3 小窗/受限网格验证 | D 整组状态机与图层开关 | 完成 v3 小窗并确认演示航区 |
| 08-25 / D12 | 冻结输入只读 | 完整风险窗复验 | v3 整组或 v2 稳定基线 | 分层渲染与重规划对比页 | D 展示闭环可用 |
| 08-26 / D13 | 双备份/恢复演练 | 回归 | 回归与性能记录 | 首次断网彩排 | G0–G4 通过 |
| 08-27 / D14 | 只读支持 | 只修阻断 | 只修阻断 | 优化图例、状态与警示 | 彩排问题清零 |
| 08-28 / D15 | 冻结证据 | 冻结参数版本 | 冻结输出 | 第二次断网彩排 | G5 通过 |
| 08-29 / D16 | 答辩材料核对 | 话术核对 | 话术核对 | 演示脚本与截图 | 答辩包成型 |
| 08-30 / D17 | 最终冻结 | 最终回归 | 最终回归 | 全量链接/摘要/状态验收 | 形成最终包 |

### 5.1 2026-08-16 实测状态更新（D3 日）

| 项 | 状态 | 说明 |
|---|---|---|
| mur/dikson 12 类数据补齐 | DONE | land_sea_mask/wave/ice_type/ice_edge 补采完成；doctor 4168 项 PASS |
| 144 h 回放 + bundle + RunContext | DONE | a-bundle-bf2ebf33681a08969a4b2dc0，1212 帧，12 类 complete |
| orchestrator intake（corridor 2.2.0 外海起点） | PASS | 走廊/时域/12 类/代次全匹配 |
| B 风险 build | PASS | 145 帧完整风险窗已提交 risk-store（r2/r3/r4） |
| v3 四层 + 重规划 | BLOCKED | 三次真实运行均止于 c_initial_planning：任何网格 ≥34 个可航未知节点（数据侧缺口，见交付说明 §15.4） |
| D 消费 v3 输出 | NOT RUN | 无 v3 输出可消费；D 骨架自测 6 tests PASS |
| 双备份 | PASS | 工作区 + /tmp 双位置备份完成，均 doctor PASS（1212 项 0 错误） |

证据与恢复点：`work_package_a/data/output/golden/EXECUTION_LOG_20260816.md`、
`run-stage-report.json`（mur-v3-smoke-20260816-r2/r3/r4）、
`最终交付说明.md §15`。

## 6. 参数与科学接口规则

必需参数按以下顺序取得：可追溯公开典型值 → 透明拟合 → 明确标注的演示默认值。海洋/气象、
冰情、船舶、航运/法规、数据/模型五类专业接口保留，但本轮不要求跨专业团队签字。

演示参数允许为了避免明显荒谬结果而调整；每次调整必须版本化并说明原因，不能写成真实船舶或
科学模型校准。

## 7. 降级与停止规则

1. 现场网络失败时使用预置数据，不把现场下载作为主链。
2. 完整真实 12 类长窗未形成时，可使用来源清楚、连续且足以演示的冻结场景；必须准确标注。
3. v3 性能不足时以 v2 三目标 + 重规划作为比赛主线，不发布不完整四层整组。
4. 任何阶段超过性能预算时取消并保存阶段报告，不无限等待。
5. 第二走廊、CNN、可选水深/法规层和新算法不得阻塞主航区 A–B–C–D。

补充停止规则（源自：ABC_10_DAY_SPRINT_归档_20260815.md）：

6. 主走廊完整候选仍无可用数据时，可尝试迁移走廊；当前实际按走廊上限交付了 tromso 144 h
  （非 96 h）；不得把旧 v1、9 类、稀疏旧 B 或 formal-shape fixture 改名成演示实源。
7. 两条走廊都失败时，保留 fixture 工程链和完整失败报告，结论写“实源验收未完成”，不使用
   现场临时拼接。
8. G1 未通过不做正式 G2；G2 未通过不做真实 G3；G3 未通过不把 v3 作为主线成果。
9. 单阶段达到安全超时或内存预算时，先取消并落盘报告；不允许无心跳无限等待。

## 7.1 CNN 与实验旁支约束（源自：ABC_10_DAY_SPRINT_归档_20260815.md）

`22_深度学习综合风险预测模型.zip` 已完成正式 B 仓库内 P1 可选实验后端整合；P2 shadow
sidecar 未完成，未进入正式 RiskFrame/store/C，是否启动是人工决策而非默认任务。

若用户另行批准 P2，只允许：显式开关、fail-open、隔离输出、超时不影响正式 build、单独指标
与保留期。它不能替换规则主线，也不能把单步预测复制为 169 小时序列。

实验 B 的 lock/Mamba/Ruff 修复只有在 G1–G3 关键路径不受影响时执行；35 tests 的现有
`.venv` 结果不能写成 Mamba+uv 验收通过。

## 8. 实验 B 顺序

只有真实主线和比赛演示闭环稳定后，才允许对 `work_package_b_experimental` 做最小卫生修复：

1. 修正 Ruff `I001`；
2. 审查 `pyproject.toml` 与 `uv.lock` 后更新 lock；
3. 重建本地 Mamba 环境；
4. 运行完整 `make check`。

不得修改正式 B，不得把实验输出写入正式 store 或 C。该任务没有到达顺序条件前保持延期。

## 9. 保留但不处理的风险

- CNN P2/P3、许可证和训练资料问题；
- 规则风险、船舶参数、hard mask 的科学与法规校准；
- 编排器长运行性能、阶段超时和失败持久报告；
- v3 整组性能与 tromso 起点未知格点阻塞（2026-08-15 实测；待项目负责人选择方向）；
- bathymetry、法律区、等待动作、D* Lite/LPA*/MPC；
- 外部源、凭据和产品目录变化。

## 10. 完成定义

- G0–G5 通过，或非主线增强项有清楚延期说明；
- 一套冻结 A 数据能驱动 B、C、D，无须比赛现场联网；
- 风险图、三目标路线和重规划在视觉与逻辑上基本合理；
- 所有模块使用同一运行身份，旧 generation 不覆盖当前显示；
- 演示脚本、失败恢复方式、参数来源和局限可供 4 名学生共同讲解；
- handoff 与 [顶层系统文档](ARCTIC_ROUTE_SYSTEM.md)口径一致；
- 不把演示结果写成真实导航或科学校准结果。

补充（源自：ABC_10_DAY_SPRINT_归档_20260815.md）：

- 每个未通过闸门都有可复现的阻断报告和明确所有者；
- 两次断网复现的输入、配置和内容摘要一致；
- 各仓标准门禁、`git diff --check`、Markdown 相对链接与归档原文保护检查通过；
- 没有把 synthetic、legacy、fixture、P1 CNN 或未校准结果描述成真实正式成果。

## 10.1 明确延期（源自：ABC_10_DAY_SPRINT_归档_20260815.md）

新训练 Transformer/CNN、Q50/Q90/概率预测、AIS/事故标签训练、真实船舶和 Ice Class 1A
校准、方向相关风浪流、净水深和法律限制区硬约束、D、MPC、D* Lite/LPA*、10/30 min 正式
输出均不属于本冲刺。是否在后续推进，必须在当前主线冻结后另立目标、所有者与验收标准。

## 11. 入口

- [系统权威](ARCTIC_ROUTE_SYSTEM.md)
- [A handoff](work_package_a/work_package_a_handoff.md)
- [B handoff](work_package_b_handoff/work_package_b_handoff.md)
- [C handoff](work_package_c/work_package_c_handoff.md)
- [编排器 handoff](arctic_route_orchestrator/arctic_route_orchestrator_handoff.md)
- [实验 B handoff](work_package_b_experimental/work_package_b_experimental_handoff.md)

本轮 Git 提交和同步由项目负责人在会话结束后手动执行；计划不再把 contracts 同步列为阻塞。
