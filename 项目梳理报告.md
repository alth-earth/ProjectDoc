# 项目梳理报告

Status: CURRENT
Last Updated: 2026-08-16
Applies To: entire project (recommended first read)
Authoritative For: project narrative and current understanding

## 1. 项目背景

北极航线预测驱动动态规划系统（挑战杯演示项目）：以摩尔曼斯克外海 → 迪克森外海
为主走廊，用环境数据生成逐小时风险场，再由时间依赖 A* 生成四层 × 三目标路线集，
并在 +6h 处重规划。目标是稳定、可断网、可复现地展示“数据变化 → 风险变化 →
路线/指标更新”的完整链路，不宣称真实导航/适航能力。

## 2. 总体目标

工程演示链路：A（数据）→ B（风险）→ C（路线+重规划）→ D（只读展示），
由 orchestrator 组织正式运行并原子发布 v3 四层整组；数据与制品可离线冻结复用。

## 3. 系统架构

```text
arctic_route_contracts（共享 corridor/scenario/vessel/RunContext）
  → A 下载/规范化/冻结 DatasetBundle.v2 + RunContext.v2
  → B 逐小时风险帧 bc.risk-frame.v2（committed window）
  → C 时间依赖 A* → cd.four-layer-route-plan-set.v3 + 6h replan
  → D 只读展示（真实制品离线消费）
  → orchestrator 编排、原子发布、阶段报告、可中断超时
```

## 4. 各工作包职责

| WP | 唯一职责 | 正式版本 |
|---|---|---|
| contracts | 共享走廊/场景/船型/时域/RunContext | 0.3.0 |
| A | 数据采集、TOPAZ originalGrid 重建、规范化、manifest、bundle | 0.4.2 |
| B | 逐小时风险、置信度、hard mask、环境速度因子 | 0.2.0 |
| C | 最终船速/ETA/成本/路径搜索/重规划/发布 | 0.4.0 |
| D | 只读展示 v3/v2 制品 | 0.1.0 |
| orchestrator | 跨包编排、intake、阶段报告、超时、原子发布 | 0.1.0 |

## 5. 数据链

`source_snapshots`（含 TOPAZ originalGrid 重建快照）→ `ready` 帧 + manifest →
DatasetBundle.v2（1212 帧，mur）→ RunContext.v2 → B committed risk window →
C v3 四层整组 → D 快照。

## 6. Contracts

`corridor.v1`（mur v2.2.0）、`scenario.v2`（murmansk_dikson_august_2026_demo_v1
v1.0.0）、`a.dataset-bundle.v2`、`run-context.v2`、`bc.risk-frame.v2`、
`cd.four-layer-route-plan-set.v3`、`orchestrator.execution-spec.v1`。

## 7. 当前 Demo RC1

2026-08-16 建立：bundle `a-bundle-32cafad4…`、RunContext run-…0b0005、
corridor 2.2.0；r6 首次完整 E2E 成功，r7 业务级确定性复现成功。
精确 ID 见 `work_package_a/docs/DEMO_RC1_BASELINE_20260816.md`。

## 8. 核心算法

- B：11 分量规则基线；未知输入 → risk NaN/confidence 0/risk_level 5；
  `land_sea_mask_plus_unknown_v1` 把 source-unknown 节点置 hard（不可规划）；
- C：state=(node, 60min bucket, heading_code)，8 邻接、无等待；等价小时成本；
  admissible heuristic = distance/max_speed×(w_travel+w_distance)；
  RiskSampler 已优化（预计算 numpy 数组 + bisect + 直接索引），
  单目标 144h ≈96s；
- 重规划：+6h observation → suffix risk commit → 同代次 v3 四层重规划原子发布。

## 9. 关键历史问题与解决过程

- TOPAZ default-part 矩形表示空洞（native originalGrid 有限）→ A originalGrid
  重建（20km 保守阈值、水掩膜、跨陆防护、矢量旋转保留）；
- 起点 coverage → corridor 2.2.0（外海起点；Dikson 终点变更非严格必要）；
- unknown-navigable=34 → 重建 + 保守 hard-mask 后 B 风险帧 unknown=0；
- C 初始规划 >1h → sampler/planner 优化至单目标 ≈96s；
- D 真实制品消费失败 → 离线本地 schema registry + layers 数组解析；
- `/tmp` 备份被重启清空 → 第二备份改为工作区同盘副本，`/tmp` 仅临时。

## 10. 当前成熟度

A/B/C/D/v3/replan/D 消费：PASS；业务级可复现：PASS；offline 审计：PASS；
可中断超时机制：PASS（单测）；worker-mode 全链：NOT RUN；独立灾备：无。

## 11. 当前冻结项

数据产品、corridor 2.2.0、scenario、risk semantics、C cost、A interpolation、
hard-mask policy、planning algorithm、RC1 artifacts。仅允许 blocker/
correctness/safety 修复。

## 12. 下一阶段

Pre-demo：worker-mode 全链冒烟、Live Demo 排练、恢复演练、独立备份。
Post-RC1：hard_reason、可选 planner 优化、更多走廊/场景。详见
`POST_RC1_PLAN.md`。

## 13. 阅读顺序

```text
DOCUMENTATION_INDEX.md
  → 项目梳理报告.md（本文）
  → ARCTIC_ROUTE_SYSTEM.md
  → CURRENT_STATUS.md
  → DEMO_RC1_BASELINE_20260816.md
  → 对应 WP README / HANDOFF / docs
  → 历史：EXECUTION_LOG_*.md、归档文件映射表
```
