# D 展示层选型评估：v2 三目标 vs v3 四层十二路线

> 评估时间：2026-08-15（Asia/Shanghai）
> 时间口径：项目整体完成 2026-08-30 前（15 天）
> 状态：**已确认（2026-08-15）**——项目负责人确认按本报告建议落地：v3 为演示主线、v2 为
> 强制后备；执行细节见下方门槛与各包文档。
> 结论摘要：倾向 **v3 四层 × 三目标（12 路线整组）为演示主线，v2 三目标为强制后备**；
> 前提是 orchestrator/C 阶段预算与心跳落地并在 8/20 前完成一次 v3 小窗验证。

## 1. 背景与固定约束

- v2：单次运行 3 条路线（`fastest`/`low_risk`/`recommended`），`cd.route-plan.v2`；
- v3：单次运行 4 层 × 3 目标 = 12 条路线整组（`full_voyage`、`main_corridor_24_72h`、
  `rolling_0_24h`、`executable_0_6h`），`cd.four-layer-route-plan-set.v3`，整组原子发布；
- 结构约束（无论选哪个）：D 的接口按 v3 Schema 预留；v2 兼容读取；同一运行禁止双写。

## 2. 五项评估

### 2.1 工作量与时间线可行性

现状：C 的 v2/v3 规划与重规划代码、Schema、原子发布均已实现并有 138 项测试；orchestrator
的 v2/v3 execution spec 均已实现。差距不在“有没有代码”，而在两点：

1. orchestrator 集成长运行未收口（此前 24 分 49 秒人工中断），阶段心跳/超时/失败报告本轮
   已补，但尚未跑完整集成验证；
2. D 展示层尚未实现，无论 v2 还是 v3 都要新写。

时间估算（4 人，8/15–8/30）：

- v2 主线：D 读取 v2 JSON/GeoJSON + 地图/指标/重规划对比 ≈ 2–4 人日；与 orchestrator 联调
  ≈ 1–2 人日；合计约 3–6 人日。
- v3 主线：D 读取 v3 整组（12 路线分层展示 + 整组状态）+ 重规划对比 ≈ 4–7 人日；联调与
  整组原子语义处理 ≈ 2–3 人日；合计约 6–10 人日。

15 天窗口内两者都可完成；v3 多出约 3–4 人日，但被“8/20 前完成 v3 小窗验证”的前提吸收。
若 8/20 前 v3 长链仍未稳定，自动回退 v2 主线，不阻塞 8/30。

### 2.2 渲染与讲解负担

- v2：3 条路线在同一张图上清晰可读，适合“一次规划 + 一次重规划”的短讲解。
- v3：12 条路线同时渲染会视觉拥挤；分层（全航程/主通道/滚动/可执行）需要图例、图层开关
  或分屏。建议演示时按层开关展示（默认全航程 + 当前滚动/可执行层），讲解“分层如何对应
  决策时域”反而是加分项，因为能体现“不同时域不同决策”的完整故事。

负担可控条件：D 必须支持图层显隐与“选中层高亮”，否则 12 条线一起画会减分。该能力约
0.5–1 人日，已计入 2.1。

### 2.3 D 消费接口复杂度

- v2：`route-plan-v2.schema.json`，3 条 plan，字段少，D 解析简单。
- v3：`four-layer-route-plan-set.v3`，4 层 × 3 plan + 整组 ID/digest/原子状态，D 需：
  校验整组完整性（12 条齐全）、按层分组渲染、处理“整组替换/无部分结果”语义。

接口本身已 Schema 化且有 codec 往返测试，复杂度差异主要在前端分组与状态机。固定约束要求
D 按 v3 Schema 预留，因此无论 v2/v3，D 都应能识别 v3 结构；选 v3 主线只是让该投入立即
生效，不是额外“预留成本”。

### 2.4 重规划闭环的可讲性

- v2：一次重规划 = “3 条路线更新 + 指标变化”，故事短，容易讲。
- v3：一次重规划 = “4 层各 3 条路线同时更新”，可以讲“同一时刻各决策时域如何一致地更新”
  ——演示价值更高，也更能体现系统“整组原子”设计。代价是需要预先设计一页对比口径
  （初始 vs 重规划的层状态矩阵），约 1 人日。

### 2.5 “12 条整组全成或全败”的风险

v3 最大风险：任一层的任一条路线失败（性能、hard mask、端点锚点不可物化），整组不发布。
缓解：

1. 8/20 前用冻结数据跑一次 v3 小窗，把最慢层/失败点提前暴露；
2. D 按“整组状态机”展示：无完整整组时显示上一完整代次并标“更新中”，不冻结页面；
3. 演示脚本准备 v2 后备：若 v3 在彩排中失败超过 2 次，立即切换 v2 主线，不在现场调试。

## 3. 建议（待定）

**建议以 v3 四层 × 三目标（12 路线整组）为演示主线，同时保留 v2 三目标为强制后备。**

理由：

1. 时间充足（15 天）且代码已实现，主要增量在 D 展示与一次 v3 小窗验证；
2. v3 的“分层决策时域 + 整组原子重规划”是比 v2 更强的答辩故事，符合挑战杯“展示系统
   能力”的取向；
3. 固定约束已要求 D 按 v3 Schema 预留，选 v3 不产生额外预留成本；
4. 风险可用“整组状态机 + v2 后备脚本”对冲，不会造成 8/30 无路可退。

落地前必须满足的门槛（下一轮确认后执行）：

- [ ] 8/18 前 orchestrator 阶段心跳/超时/失败报告通过小窗 smoke（本轮已完成代码与单元
  测试，待集成验证）；
- [ ] 8/20 前用 `tromso_isfjorden_august_2026_demo_v1` 冻结数据完成一次 v3 小窗或受限
  网格整组运行，记录各层耗时；
- [ ] D 实现分层渲染与整组状态机，并准备 v2 后备页面；
- [ ] 演示脚本把“初始 vs 重规划”的层状态矩阵做成固定一页。

若任一门槛未达，回退 v2 主线，且不把 v3 未完成写成演示缺陷。

## 4. 附：v2/v3 对照表

| 维度 | v2 三目标 | v3 四层十二路线 |
|---|---|---|
| 路线数 | 3 | 12（4 层 × 3） |
| 发布语义 | 单批 3 条 | 整组原子，全成或全败 |
| 新增 D 工作量 | 3–6 人日 | 6–10 人日 |
| 渲染负担 | 低 | 中（需图层开关） |
| 答辩故事 | 简洁 | 更强（分层决策时域） |
| 失败风险 | 低 | 中（可后备对冲） |
| 状态 | 强制后备 | 推荐主线（待确认） |
